{% extends 'base.html.twig' %}

{% block title %}Page d'ajout d'atelier{% endblock %}

{% block body %}

{{form_start(form)}}
{{form_rest(form)}}
{% if id is not null %}
<button class="btn btn-primary">Modifier</button>
{% else %}
    <button class="btn btn-primary">Ajouter</button>
{% endif %}

{{form_end(form)}}
{% endblock %}
